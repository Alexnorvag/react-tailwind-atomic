// header + post

import { Header } from "../containers(organisms)/header";

export const PrimaryLayout = () => {
  return (
    <div>
      <Header />

      <div>post container here</div>
    </div>
  );
};
